//Ternary Operator
//its frequently used as a substitute for if else statement, this is the only operator that takes 3 operands.
//Syntax: condition ? expression if True : expression If false


//program to check pass or fail
let obtained_markes = 87

// check the condition 
let result_value = (obtained_markes >= 40) ? "pass" : "fail";

console.log (`You ${result_value} the exam.`);